//
//  CompletedChoreListView.swift
//  EarnIt
//
//  Created by Ryan Jones on 11/23/20.
//

import SwiftUI

struct CompletedChoreListView: View {
  var body: some View {
    VStack {
      
    }
    /*
    NavigationView {
      VStack {
        if #available(iOS 14.0, *) {
          List {
            ForEach(self.completedChoreStore.completedChores, id: \.id) {
              ChoreTypeRowView(chore: $0)
            }
          }
          .listStyle(InsetGroupedListStyle())
        } else {
          List {
            ForEach(self.completedChoreStore.completedChores, id: \.id) {
              ChoreTypeRowView(chore: $0)
            }
          }
          .listStyle(InsetGroupedListStyle())
        }
      }
      .navigationBarTitle(Text("$0.00 Earned"))
      .navigationBarItems(trailing:
        HStack {
          VStack {
            Button(action: {}) {
              Image(systemName: "envelope")
            }
          }
        }
      )
    }
 */
  }
}

/*
struct CompletedChoreListView_Previews: PreviewProvider {
  static var previews: some View {
    CompletedChoreListView()
  }
}
*/
